import XCTest
@testable import swiftui_previews

final class swiftui_previewsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swiftui_previews().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
